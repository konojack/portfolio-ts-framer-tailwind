/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // image: {
  //   domains: ['media.licdn.com'],
  // },
  images: {
    domains: ['media.licdn.com'],
  },
};

module.exports = nextConfig;
